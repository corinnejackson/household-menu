{
  "name": "household-menu",
  "private": true,
  "description": "Family meal & baby nutrition planner, served by a Cloudflare Worker",
  "scripts": {
    "dev": "wrangler d1 migrations apply DB --local && wrangler dev",
    "deploy": "wrangler d1 migrations apply DB --remote && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.136.1"
  }
}
